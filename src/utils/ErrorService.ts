/**
 * This file serves as a wrapper to access the error handling functionality
 */
import errorHandlingService from './ErrorHandlingService';

// Just re-export the service
export const errorService = errorHandlingService;
