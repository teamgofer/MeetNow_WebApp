// Export all UI POC components from this index file
// This makes it easy to import them elsewhere like:
// import { AnimatedButton, UiDemo } from '@/features/ui-poc';

export { default as AnimatedButton } from './AnimatedButton';
export { default as UiDemo } from './UiDemo';

// When adding new components, export them here as well
